{$DOMAIN} {
    request_body {
        max_size 32MB
    }
    reverse_proxy app:3000 {
        flush_interval -1
    }
    # Access logging is deliberately not enabled: never log Authorization or request bodies.
}
